export default function EyeSpyPage() {
  return (
    <main className="mx-auto max-w-[1400px] px-6 py-14">
      <h1 className="text-3xl font-bold text-ink">EyeSpy</h1>
      <p className="mt-3 max-w-xl text-ink/60">
        Not built yet — full spec lives in <code>EYESPY.md</code> at the repo root. This
        page is a placeholder so the nav is already in the right shape once it's
        scaffolded.
      </p>
    </main>
  );
}
